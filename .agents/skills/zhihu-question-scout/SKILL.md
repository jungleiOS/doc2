---
name: zhihu-question-scout
description: 用 Kimi WebBridge 抓取知乎「邀请回答」和「推荐问题」列表，结合本仓库已有文章的选题领域，每次为用户筛出 3 个最值得回答的问题（选题精评含问题生命周期筛查——死问题/枯竭问题硬否决——与好物适配评估，调用 zhihu-haowu-evaluator；带货回答默认延迟插卡冷启动），确认题目后调用 zhihu-answer-drafter 起草回答初稿。当用户说「选题」「帮我选问题」「看看知乎邀请」「推荐问题」「今天答什么」「有什么可答的问题」时触发。
---

# 知乎选题助手（zhihu-question-scout）

从用户的知乎邀请回答 + 创作中心推荐问题中，基于「曝光份额 × 机会 × 供给 × 问题类型」四因子模型（见第 5 步），每次选出 **3 个** 期望收益最高的问题。本 skill 只管选题；确认题目后的起草流程在 **zhihu-answer-drafter** skill。

## 工作流程

### 1. 浏览器通道检查（必做）

优先用 **ego-browser**（2026-08-22 起实测为主力通道，用户真实浏览器、登录态在）：

```bash
ego-browser nodejs <<'EOF'
const ts = await useOrCreateTaskSpace('zhihu-scout');   // 按名字幂等获取，不用记 id
const tab = await openOrReuseTab('https://www.zhihu.com/creator/featured-question/invited', {wait:true});
await new Promise(r=>setTimeout(r,3000));
const out = await js(`document.title`);
console.log(out);
EOF
```

注意：ESM 环境（`import fs from 'fs'` 可用，不能 require）；工作目录不是仓库根目录，读写文件用绝对路径；`js()` 里的模板字符串在 heredoc 中会被外层再解析一层，避免在 js 代码字符串里嵌套 `\n` 字面量。用户接管过浏览器后命令会报 hard stop，等用户说继续后 `claimTaskSpace(id)` 恢复。

备用通道 **WebBridge**（浏览器无窗口时会报 `No current window`，此时直接换 ego-browser）：

```bash
~/.kimi-webbridge/bin/kimi-webbridge status
```

要求 `running: true` 且 `extension_connected: true`，否则按 kimi-webbridge skill 的 `references/operations.md` 处理。后续所有请求用 session 名 `zhihu-scout`。

### 2. 建立用户选题画像

仓库文章按「归集目录」组织（如 `多口充电器选购指南/` 内含主文、衍生回答、子级 `drafts/`）。快速扫描以下位置提炼用户擅长领域、已有观点和已答主题：

- 根目录的 `*.md`
- 各文章归集目录（含其子级 `drafts/`）
- `drafts/`、`published/`（如有）

只读标题和各级 `#`/`##` 标题，不要全文通读。画像要点：核心领域（如充电/数码/AI 硬件）、已覆盖主题（避免重复）、可复用的实测案例。

另需加载**粉丝画像**（`analytics/` 下有最新数据报告时读取其粉丝画像节），选题角度优先锚定粉丝已有兴趣点。当前画像（2026-08-31 复盘）：25 岁以下占 60.5%、涨粉主力为 iPad/教育优惠类苹果生态内容、数码 3C 未进兴趣 TOP15——泛 3C 选题（如「充电宝电芯哪种好」）对粉丝没有推送基础，应改造为苹果生态 + 学生场景角度（如「教育优惠买的 iPad 配什么充电宝」「MacBook 宿舍一个充电器搞定全部设备」）；角度改造不了的泛 3C 题在「建议切入角度」里如实说明风险，由用户定夺。

### 3. 抓取问题列表

知乎创作中心「问题推荐」页有两个标签（已实测可直接抓取）：

- 邀请回答：`https://www.zhihu.com/creator/featured-question/invited`
- 推荐问题：`https://www.zhihu.com/creator/featured-question/recommend`

对每个页面：navigate → sleep 3~4 秒 → **先滚动加载到 40 条**再提取。

滚动加载（2026-08-28 起，候选量从 20 增至 40）：列表是无限滚动加载，用下面的脚本滚动到目标条数或到底（滚动由页面自身触发，不额外刷新页面，风控成本与手刷一致）：

```js
(async()=>{const q=()=>[...document.querySelectorAll("a[href*=\"/question/\"]")].filter(a=>/\/question\/\d+$/.test(a.getAttribute("href")||"")).length;let last=0;for(let i=0;i<10;i++){window.scrollTo(0,document.body.scrollHeight);await new Promise(r=>setTimeout(r,1500));const n=q();if(n>=40||n===last)break;last=n;}return q()})()
```

然后用下面的 evaluate 脚本提取（已实测可用）：

```js
(()=>{const qs=[...document.querySelectorAll("a[href*=\"/question/\"]")].map(a=>({t:(a.innerText||"").trim().replace(/\n+/g," ").slice(0,80),h:a.getAttribute("href")})).filter(x=>x.t.length>5&&/\/question\/\d+$/.test(x.h||""));const seen=new Set();const out=[];for(const x of qs){if(!seen.has(x.h)){seen.add(x.h);out.push(x);}}return JSON.stringify(out)})()
```

注意：

- 通过 curl 调用 WebBridge 时，JS 需做 JSON 转义。
- 返回空数组时先 `snapshot` 确认是否登录墙（是则提示用户先在浏览器登录知乎）。
- 两个来源合并后按问题 ID（URL 中的数字）去重；邀请的问题优先级高于推荐。
- **主动搜新问题（第三来源，2026-08-31 起）**：邀请/推荐列表里大多是衰退期老问题。两个列表粗筛后存活 <5 个时，用知乎搜索领域关键词（如「充电宝」「充电器」）按时间排序，或看「数码」话题的实时流，打捞**诞生期/爆发期问题**——提出 ≤7 天、已有 3-8 个回答、关注者 >20 且在增长。新问题在 24h 内有热榜/推荐爆发窗口，是冷启动红利区；等问题进入死寂期再答，等于在废墟里捡流量。

### 4. 历史去重

读取 `outlines/选题历史.md`，提取所有问题 ID，从候选列表中剔除——**同一问题 ID 选过的和标注「已弃选」的都不再推荐**。文件不存在则视为无历史。

注意（2026-08-28 起）：去重只在**问题 ID 层面**做。**主题级近重复不再剔除**——撞已答主题或内容部分重叠的题仍可进入精评和最终选题单，但必须按 5.6 的规则注明。

### 5. 选题模型：曝光份额 × 机会 × 供给 × 问题类型

**第一性原理推导**：一篇回答的期望收益 = 我的回答能被多少人看到 × 看到后愿意互动的概率 − 写作成本。2026-09-04 算法重构（依据：39 篇回答全量回测，详表见 `/tmp/zhihu-all-answers.json`）：

- **D 曝光份额（每条回答能分到多少曝光）**：核心指标 = 浏览量 ÷ 回答数。10 万浏览 200 回答的题，每条只分到 500 曝光；1000 浏览 3 回答的题，每条分到 333。累计浏览量和关注者数只作辅助参考——看问题的人多不代表会看你的回答，落到后面给谁看？
- **O 机会（竞争格局）**：回答数少、头部回答赞数低 = 前排空缺。但低 D 题的前排空缺是陷阱（没人看的题排第 1 也没用），O 在低 D 时封顶。
- **M 供给（素材复用度）**：降低权重（1-3 分）。回测发现 M=5（素材直接复用）的题没有比 M=3 的题表现更好——内容复用降低写作成本，但不决定读者买不买账。
- **Q 问题类型（最强预测因子，2026-09-04 新增）**：知识类篇均阅读 425 / 赞同 3.3，推荐类仅 171 / 0.9——相差 2.5 倍 / 3.7 倍。问题类型比主题更能预测表现。

**总分 = D × O × M × Q（乘法，满分 300）**。用乘法：任一因子接近零，期望收益就是零。

#### 5.1 硬否决（任一命中即淘汰，不打分）

- 用户没有一手经验的边界领域（医疗/法律/金融投资等，遵守根 AGENTS.md）
- **超出用户本人知识储备、用户无法为内容站台的题**（2026-08-23 实战教训：「5V USB 口测出 90 多伏」涉及 Y 电容/安规等电路底层知识，稿写完了用户才说自己不懂、不敢发，整篇作废。选题时不能只看仓库素材覆盖度，还要看用户本人是否懂这个细分领域；拿不准就在选题单里标注知识门槛，让用户先确认）
- **死问题：关注者 <6，直接否决**（2026-09-04 调整：原 100→6。实测 100 门槛杀伤过大，当前邀请/推荐池 80 题中 0 题过 100 关注；历史数据中关注 6-50 的题也有跑出 500+ 阅读的case。关注 ≥6 是问题有最低可发现性的实证底线）
- **曝光份额过低：浏览 ÷ 回答 < 50，直接否决**（2026-09-04 新增。即使关注者 >100，如果浏览/回答 <50，说明每条回答分到的曝光极少，排前排也没意义。实证：性价比高的充电宝推荐 2800浏览÷36回答=78，篇均阅读 37）
- **回答数过多：>50 条回答，直接否决**（2026-09-07 调整：原 >25。46 篇实测：30-40 回答的大题排 20-30 名仍分到 400-1000 阅读，大题是权威投资不是放弃。≤10 回答好冲前排，11-30 甜蜜区，31-50 流量可观但排不进前 10，>50 沉底风险过高）
- 问题已死亡或流量枯竭：最近 6 个月无新回答，且不是「xx 怎么选」类经典长尾搜索题。**累计浏览量只代表历史，不代表当下日活**——浏览 >1 万但回答数 <10 且近 30 天无新回答的题，是流量高峰已过的「遗迹」（复盘实证：10.8 万浏览的「快充协议」题仅 6 个回答，排第 4 阅读只有 8；若有流量早有几十人来答），同样否决
- 读完问题描述后发现题主诉求与标题不符、或实际主题与本仓库领域无关

#### 5.2 两阶段漏斗

- **阶段 A 粗筛（只看标题和来源，不打开问题页）**：凭领域匹配快速排除，保留 ≤8 个进入精评。近重复主题不排除，带上撞题标注进入精评（见 5.6）。
- **阶段 B 精评（逐个打开问题页）**：navigate → sleep 3 秒 → 先抓统计数据，再按 zhihu-answer-drafter 第 2 步的脚本抓问题描述（同一次访问完成两件事）。

统计抓取脚本（已实测：问题页数据区文本为「关注者\n数字\n被浏览\n数字」，用 `body.innerText` + 正则，对 DOM 改版更鲁棒；topUps 为 2026-08-29 新增，按 DOM 顺序取默认排序前 5 条回答的赞数，回答卡片选择器如失效需实测校准）：

```js
(()=>{const t=document.body.innerText;const num=re=>{const m=t.match(re);if(!m)return 0;const v=parseFloat(m[1].replace(/,/g,""));return Math.round(v*(/万/.test(m[0])?1e4:1))};const ups=[...document.querySelectorAll(".ContentItem.AnswerItem, .List-item")].map(el=>{const m=(el.innerText||"").match(/([\d.,]+万?)\s*人赞同了该回答/);if(!m)return null;const v=parseFloat(m[1].replace(/,/g,""));return Math.round(v*(/万/.test(m[0])?1e4:1))}).filter(v=>v!==null);return JSON.stringify({follow:num(/关注者\s*([\d.,]+万?)/),view:num(/被浏览\s*([\d.,]+万?)/),answers:num(/([\d.,]+万?)\s*个回答/),topUps:ups.slice(0,5)})})()
```

抓不到数字时用 `snapshot` 人工读取「关注者 / 被浏览 / x 个回答」三个数；`topUps`（默认排序前 5 条回答的赞数）抓不到就人工读前 5 条的赞数，都不打 0 分。

阶段 B 另需记录**问题生命周期信号**（2026-08-31 起）：问题创建时间（问题日志可见）和最近一条新回答的日期（问题页切「按时间排序」看首条）。这两个值是 5.1 枯竭判定和 5.3 D 加分的依据，写入选题单数据快照和第 7 步历史表。

#### 5.3 打分细则（2026-09-04 重构）

**D 曝光份额（1-5 分）**——量「每条回答能分到多少曝光」，不量问题总流量：

核心指标 = **浏览量 ÷ 回答数**。辅助信号：近 7 天有新回答（证明题还活着，算法还在推）。

| 分 | 浏览÷回答 | 且 |
|----|-----------|-----|
| 5 | ≥5000 | 近 7 天有新答 |
| 4 | ≥1000 | 近 30 天有新答 |
| 3 | ≥200 | 近 30 天有新答 |
| 2 | ≥50 | —— |
| 1 | <50 | —— |

注意：硬否决已在 5.1 拦截了 浏览÷回答 <50 的题，所以 D=1 的题是刚过否决线的边缘题。关注者和累计浏览量只作辅助参考，不进主公式。

**O 机会（1-5 分）**——门槛赞数 T = 默认排序第 5 名回答的赞数（topUps 第 5 个值；回答不足 5 条时 T=0）：

| 分 | 条件 |
|----|------|
| 5 | T ≤ 5（第 5 名个位数赞，或不满 5 条回答） |
| 4 | T ≤ 50 |
| 3 | T ≤ 500 |
| 2 | T ≤ 2000 |
| 1 | T > 2000 |

修正项：

- **低 D 封顶**：D ≤ 2 时，O 封顶 3 分。低曝光题的前排空缺是陷阱——没人看的题排第 1 也没用（实证：3 个关注者的题排第 1，阅读 7）。
- **回答数修正**（2026-09-07 更新）：回答数直接影响冲前排难度——≤10 回答 O +1（封顶 5），11-30 回答不调整（甜蜜区），31-50 回答 O -1（大题流量可观但前排固化）。硬否决已拦截 >50。
- **垄断但后排空虚不降档**：top1 赞数很高但 T 很低（如第 1 名 2000 赞、第 5 名 20 赞），按 T 值正常打分，不因头部赞数高降档。
- **能力对照（先验，2026-09-07 更新）**：账号 ~4000 粉但权重衰减，当前能力先验 **篇均 284 阅读 / 2.2 赞同**（依据：08-18 以来 46 篇全量数据，中位数 1 赞，P75 = 3 赞）。T ≤ 50 仍是主战场；T ≤ 500 需内容质量明显超出现有前排才选；T > 500 默认不选。

**M 供给（1-3 分）**——降权，不再隐含「成本低 = 质量高」的假设：

| 分 | 条件 |
|----|------|
| 3 | 领域内，有差异化角度可写（不是简单搬运已有内容） |
| 2 | 核心观点已有素材可复用，但需补新角度 |
| 1 | 擦边领域或完全无积累 |

先用 Grep 以问题关键词检索各归集目录和 `drafts/`，命中哪些文件决定档位，不凭印象。

**Q 问题类型（乘数 0.3 / 1.0 / 1.5 / 2.0）**——最强预测因子，按标题关键词判定：

| Q 值 | 问题类型 | 标题特征 | 实证篇均（46 篇） |
|------|----------|----------|---------|
| 2.0 | 知识/科普类 | 为什么、是不是、真的吗、什么原理、有什么区别 | 阅读 425 / 赞同 3.3 |
| 1.5 | 决策/对比类 | 该不该、有必要吗、够用吗、值得买吗、A vs B | 阅读 274 / 赞同 3.0 |
| 1.0 | 经验/方法类 | 如何、怎么做到、有什么技巧、经验分享 | 待积累，取中位 |
| 0.3 | 推荐/选购类 | 怎么选、哪个好、推荐、哪款、买什么 | 阅读 142 / 赞同 0.7（20 篇，6 篇 0 赞） |

判定规则：以标题主句型为准，不看描述。标题同时含多种句型时（如「怎么选，是不是越贵越好」），取信息量更高的那个（知识 > 决策 > 推荐）。

**主题维度观察（2026-09-07 新增，非正式因子）**：主题方向的影响大于问题类型。苹果生态类（Mac/iPad/iPhone 选购决策）13 篇篇均阅读 489 / 赞同 3.2，充电器配件类 33 篇仅 202 / 1.7——差 2.4 倍 / 1.9 倍。粉丝画像（25 岁以下 60.5%、涨粉主力 iPad/教育优惠）进一步放大苹果类优势。**选题角度优先锚定苹果生态 + 学生场景**；泛充电器/充电宝题除非能改造为苹果生态角度（如「MacBook 宿舍一个充电器搞定」），否则预期打折。

**排序规则**：总分 = D × O × M × Q，取前 3；并列时邀请 > 推荐，再并列比近期活跃度（最近新答日期更近者优先）。

#### 5.4 校准回路（科学方法的闭环）

选题历史表记录打分时的「关注/浏览/回答数/T/最近新答」快照（见第 7 步）。回答发布后回采两个时点：**24 小时赞数**（检验冷启动测试曝光给不给量）和 **7 天赞数 + 当时排名**（稳态结果），在历史表追加「复盘」行。回采调用 **zhihu-answer-tracker** skill 完成（创作中心批量采五指标 + 问题页抓排名 + 回写复盘行）。

复盘数据维护两个量：

- **能力先验**（我方回答能拿到的赞数区间）：初始值 5–20 赞（2026-08-29，依据 500–5000 粉账号 + 3 条复盘样本 1/6/1 赞），攒到约 10 条复盘后按实际分布更新，并同步修订 5.3 的「能力对照」；
- **分档阈值**：若实际排名系统性优于/差于 O 档预期（如 T ≤ 50 也持续打不进前 5），调整 5.3 的 T 分档。

**2026-08-31 全量复盘结论**（12 篇带货回答 + 账号级数据，详表见 `analytics/带货数据采集-2026-08-31.md`）：

- 死问题实证：关注者 <10 的 6 篇（含 3 篇排名第 1）总阅读仅 53 → 已固化为 5.1「关注者 <20 硬否决」（09-04 进一步升级为 <100）。
- 枯竭遗迹实证：10.8 万 / 3.1 万 / 1.1 万累计浏览的三题，排名 4/4/9，阅读仅 8/19/20 → 累计浏览量不再作为当下流量依据，只看近期活跃度。
- 带货隔离：12 篇带货回答合计仅 166 阅读（占 2.5%），「商品卡商业降权 + 0 互动冷启动失败」双重降权成立 → 起草侧默认延迟插卡。
- 粉丝错配：25 岁以下 60.5%、兴趣 TOP15 无数码、涨粉主力为 iPad/教育优惠内容 → 选题角度锚定规则（第 2 步）。
- 频率与模板红线：8 天 12 篇 + 统一模板，疑似触发营销号检测 → 起草侧发布频率与结构差异化约束（见 zhihu-answer-drafter）。

**2026-09-04 算法重构复盘**（39 篇回答全量回测）：

- **评分-表现反相关**：高分组（≥70 分）篇均阅读 198 / 赞同 0.8，中分组（40-69）篇均阅读 385 / 赞同 3.3——旧 D×O×M 模型高分组反而表现最差。
- **问题类型是最强预测因子**：知识类（8 篇）篇均阅读 425 / 赞同 3.3，决策类（3 篇）274 / 3.0，推荐类（13 篇）171 / 0.9。→ 新增 Q 因子。
- **D 因子失真**：重构为「浏览 ÷ 回答」量每条回答的曝光份额。
- **M 因子伪相关**：降权至 1-3 分。
- **新公式回测验证**：D×O×M×Q 排序与实际表现高度吻合。

**2026-09-07 全量更新**（46 篇回答，数据存 `analytics/answers-2026-09-07.json`）：

- **能力先验上调**：篇均 200→284 阅读，2→2.2 赞同（中位数 1，P75=3）。80% 回答 ≤3 赞，仅 2% 达 9+。
- **推荐类天花板实证**：20 篇推荐类篇均阅读 142 / 赞同 0.7（6 篇 0 赞），Q 从 0.5 降至 0.3。
- **主题 > 问题类型**：苹果生态类 13 篇篇均阅读 489 / 赞同 3.2，充电器配件类 33 篇仅 202 / 1.7。选题角度优先锚定苹果生态 + 学生场景。
- **大题价值重估**：>30 回答的题排 20-50 名仍分到篇均 502 阅读。硬否决从 >25 放宽到 >50，保留大题做权威投资。
- **细水长流 ≠ 小题**：长尾取决于问题类型（知识/决策类有长尾），不取决于题目大小。推荐类小题既没前排价值也没长尾。
- **发布节奏**：日发 4+ 篇时数据被稀释（08-30 发 6 篇篇均阅读 310 vs 09-03 发 2 篇篇均 823）。少而精 > 多而散。
- **选题优先级**：① 苹果+购买决策类中竞争题（20-40 答）② 充电器/电池知识类小题（长尾保底）③ 减少推荐类小题。

闭环即：**测量 → 假设 → 验证 → 迭代**。

#### 5.5 好物适配评估（阶段 B 顺带完成）

阶段 B 打开问题页时已拿到标题、描述和数据快照，逐题调用 **zhihu-haowu-evaluator** skill，输出三档结论：推荐插 / 可插不优先 / 不插（附一句话理由和商品方向）。

- 好物结论**不进 D×O×M 总分**——好物是变现维度，不是流量维度，混进总分会扰乱选题排序。
- 总分并列时，优先「推荐插」的题。
- 结论写进选题单（第 6 步），供用户决定选品方向和插卡时机（起草侧不再代执行插卡）。
- **插卡时机默认延迟（2026-08-31 起）**：带商品卡的回答遭商业降权，叠加 0 互动冷启动失败直接沉底。默认策略为**首发不带卡、纯干货抢前排，发布 48h 后若排名进前 5 或赞数达到能力先验区间，再编辑插入商品卡**。仅当问题本身是强购买意图（题主明码求购，如「预算 200 求推荐充电宝」）且好物评估为「推荐插」时，才允许首发直接插卡。

#### 5.6 重复主题注明机制（2026-08-28 起）

主题撞题不再淘汰，但**入选时必须特别注明**，让用户知情决策：

- **完全重复**：与已发布/已有草稿主题相同 → 选题单里标注「⚠ 重复主题：与《xxx》撞题」，并写清新的切入角度是什么；想不出新角度才在「注意」里说明风险，由用户决定答不答。
- **部分重叠**：部分内容可复用但问题侧重不同 → 标注「⚠ 部分重叠：与《xxx》重叠在 xxx 部分」，写清本题侧重点是哪里、重叠内容怎么换说法（避免两篇回答文字雷同被平台判重复内容）。
- 注明格式固定在选题单的「注意」字段，前缀 `⚠ 重复主题` / `⚠ 部分重叠`，一眼可辨。
- 近重复题仍记入 `选题历史.md`（标注撞题对象），但不再写「不再推荐」——它的作用是供下轮选题时查撞题、写注明。

### 6. 输出选题单

```markdown
## 本次选题（共 N 个候选，粗筛 K 个，精评后选出 3 个）

### 1. [问题标题](问题URL)
- 来源：邀请 / 推荐
- 数据快照：关注 x / 浏览 x（累计）/ 回答数 x / 浏览÷回答 x / 创建于 x / 最近新答 x
- 评分：D=? O=? M=? Q=? → 总分 ?（满分 300）
- 好物评估：推荐插（商品方向）/ 可插不优先 / 不插（一句话理由，按 zhihu-haowu-evaluator 结论）；插卡时机：48h 后延迟插（默认）/ 首发直插（仅强购买意图题）
- 匹配理由：……（必须引用具体的已有文章文件名）
- 建议切入角度：……（指明可复用哪篇文章的哪些观点）
- 注意：……（如有事实核查点或风险；撞题题必须以 `⚠ 重复主题` / `⚠ 部分重叠` 前缀开头，见 5.6）
```

然后询问用户：要对哪些题目起草回答？确认后调用 **zhihu-answer-drafter** skill 起草。

### 7. 记录与收尾

落选/撞题记录（选题侧职责）：

- 用户明确排除的题目：不起草，把问题 ID 记入 `outlines/选题历史.md` 并在标题后标注「（已弃选，不再推荐）」。
- 浏览中遇到的**与已答主题近重复的题**也顺手记入历史（标注撞题对象，如「近重复：xxx」），供下轮选题写注明用——**不要再标「不再推荐」**，近重复题按 5.6 仍可入选。

把本次选出的问题追加到 `outlines/选题历史.md`（含打分时的数据快照，供 5.4 校准回路回采）：

```markdown
| 日期 | 问题ID | 标题 | 来源 | 关注 | 浏览(累计) | 回答数 | 浏览÷回答 | T(第5名赞) | 最近新答 | Q类型 |
|------|--------|------|------|------|------|--------|-----------|------------|----------|-------|
| 2026-09-04 | 1961371990993663235 | 华为手机+联想笔记本，求推荐一个能共用的100W充电器？ | 邀请 | 152 | 3.1万 | 8 | 3875 | 2 | 2026-08 | 推荐0.5 |
```

旧记录保持原列不动，新行追加数据列（「浏览÷回答」和「Q类型」列 2026-09-04 起新增，旧行留空即可）。回采时在同一问题下方追加一行，来源列写「复盘」，标题列写「24h 赞同 x / 7d 赞同 x / 排名 x/N」；24h 和 7d 分两次回采时，第二次就地更新同一复盘行，不要重复加行。48h 延迟插卡的题，复盘行追加「已插卡/未插卡」标记。

然后关闭浏览器会话，按本次实际使用的通道二选一：

- **ego-browser（主力）**：`completeTaskSpace('zhihu-scout', { keep: false })`，必须放在独立最后一个 heredoc 里执行，且只在前序输出确认任务真正完成后才调用（细节见 ego-browser skill）。
- **WebBridge（备用）**：

```bash
curl -s -X POST http://127.0.0.1:10086/command -d '{"action":"close_session","args":{},"session":"zhihu-scout"}'
```

## 禁忌

- 不替用户实际提交/发布任何回答，只做选题；起草规则见 zhihu-answer-drafter。
- 禁止推荐关注者 <6 的问题；禁止推荐 浏览÷回答 <50 的问题；禁止推荐回答数 >50 的问题（2026-09-07 调整：原 >25）；禁止把累计浏览量当作当下流量依据，D 打分以「浏览÷回答」为主指标。
- 不编造问题数据；抓取失败就如实说明，不假装看到了列表。
- 打分必须有抓到的数据支撑（D/O 按快照对档，M 按已有文件对档），禁止凭感觉拍分；数据抓不到就如实标注并降级处理。
- 选题理由必须引用具体的已有文章文件名，禁止空泛的「和你的领域匹配」。
